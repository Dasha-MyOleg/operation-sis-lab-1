#include <stdio.h>
#include <string.h>
#include "allocator.h"

int main() {
    printf("Testing memory free...\n");

    // ????????? ????????? ?????? ???'???
    void *ptr1 = mem_alloc(150);
    void *ptr2 = mem_alloc(300);
    void *ptr3 = mem_alloc(450);

    mem_show();

    // ?????????? ?????? ?????
    printf("\nFreeing block 2 (300 bytes)...\n");
    mem_free(ptr2);
    mem_show();

    // ?????????? ????? ?????? ?? ????????? ??'???????
    printf("\nFreeing blocks 1 and 3 (150 & 450 bytes)...\n");
    mem_free(ptr1);
    mem_free(ptr3);
    mem_show();

    // ????????? ??????? NULL ?????????
    printf("\nFreeing NULL pointer (should do nothing)...\n");
    mem_free(NULL);
    mem_show();

    printf("\nMemory free testing completed successfully.\n");
    return 0;
}
